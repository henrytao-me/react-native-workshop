import { Theme } from 'react-native-mdcore'

export default Theme.extend({
  palette: {
    transparent: 'transparent',
    primary: '#006f7b',
    primaryDark: '#005a64',
    primaryLight: '#7fb7bd',
    accent: '#ff5a5f',
    accentDark: '#cf494d',
    accentLight: '#ffacaf'
  }
})
